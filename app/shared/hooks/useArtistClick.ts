import { IArtist } from "app/shared/models/interfaces";
import toast from "react-hot-toast";

export const handleArtistSelect = async (artist: IArtist) => {
    toast("Artist page is not ready yet", { icon: "😓" });
};
